
  # Solaris Cartagena Stays

  This is a code bundle for Solaris Cartagena Stays. The original project is available at https://www.figma.com/design/n57wRDK5OX7MSg51vsRHrq/Solaris-Cartagena-Stays.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  